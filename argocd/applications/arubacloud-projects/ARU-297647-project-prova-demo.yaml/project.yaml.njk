apiVersion: arubacloud.com/v1alpha1
kind: Project
metadata:
  name: {{ name }}
  namespace: {{ namespace }}
spec:
  tenant: {{ tenant }}
  description: {{ description }}
  tags:
{% if tags and tags.length %}
{% for tag in tags %}
  - {{ tag }}
{% endfor %}
{% endif %}
  default: {{ default }}
